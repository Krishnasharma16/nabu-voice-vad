# NABU Render Voice Server V2
Uses `@ericedouard/vad-node-realtime` for realtime Silero VAD.
Build: `npm install`
Start: `npm start`
Health: `/health`
Environment: `GEMINI_API_KEY` (required), optional `GEMINI_MODEL`.
WebSocket: `/live`.
